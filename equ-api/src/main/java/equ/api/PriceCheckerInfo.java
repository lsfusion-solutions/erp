package equ.api;

public class PriceCheckerInfo extends MachineryInfo {

    public PriceCheckerInfo(boolean enabled, Integer numberGroup, Integer number, String nameModel, String handlerModel, String port, String directory) {
        super(enabled, numberGroup, number, nameModel, handlerModel, port, directory);
    }
}
