MODULE ProductionOutputPurchaseInvoice;

REQUIRE ProductionOutput, ProductionOrderPurchaseInvoice;

NAMESPACE Production;

inPriceListLedgerBatch(l, b) += orderOutputDetailProductDetail(l) == b;
