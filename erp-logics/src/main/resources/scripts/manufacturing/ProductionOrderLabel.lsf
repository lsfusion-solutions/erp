MODULE ProductionOrderLabel;

REQUIRE Label, ProductionOrder, ProductionOutput;

NAMESPACE Production;

//// ------------------------------------- Печать ценников ------------------------------ //

@Label.implementPriceTransactionDocument(Order);
batchProductDetail(d)= orderOutputDetailProductDetail(d);

@defineDocumentLabelTransaction(order, product, sku, productsStock);

@extendFormDocumentLabelTransaction(orders, o, order, nameOperationOrder, machineryContainer);