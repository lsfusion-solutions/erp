MODULE PricingPurchaseReturnBy;

REQUIRE PricingPurchaseReturn, PricingInvoiceBy;

NAMESPACE PurchaseReturn;

@defineInvoicePricingByAggregation(dumb);

// Записываем необходимо ли показывать цену надбавки в рублях по умолчанию из операции расценки, заданной для операции закупка
showMarkupPriceUserInvoice (invoice) <- showMarkupPriceOperation(pricingOperationOperation(operationUserInvoice(invoice)))
    WHEN CHANGED(operationUserInvoice(invoice));