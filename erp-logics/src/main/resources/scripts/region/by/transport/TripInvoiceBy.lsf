MODULE TripInvoiceBy;

REQUIRE TripInvoice, TripBy, InvoiceConsignmentBy;

NAMESPACE TripInvoice;

EXTEND FORM printWaybillBy
    OBJECTS i=Invoice
    PROPERTIES (i) seriesNumberInvoice, dateInvoice, addressSupplierStockInvoice, addressCustomerStockInvoice
    FILTERS inTripInvoice(t, i)
;

//Invoice.defaultArrivalTimeInvoice(i) += dateTimeTrip(TripInvoice.tripInvoice(i));
//Invoice.defaultDowntimeInvoice(i) += downtimeTrip(TripInvoice.tripInvoice(i));