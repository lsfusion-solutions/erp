MODULE RepricingLabel;

REQUIRE Repricing, Label;

NAMESPACE Repricing;

// ------------------------------------- Печать ценников ------------------------------ //

@Label.implementPriceTransactionDocument(Repricing);
Label.skipPriceTransactionDocument(document) += skipChangeLedgerRepricing(document);

@defineDocumentLabelTransaction(repricing, sku, departmentStore);
@extendFormDocumentLabelTransaction(repricings, p, repricing, skipChangeLedger, machineryContainer);

createAttributeRepricingDetailLabelTransactionDetail (r, t) += ACTION (r, t) {
    retailPriceLabelTransactionDetail(t) <- curRetailPriceRepricingDetail(r) WHERE NOT overRetailPriceLabelTransactionDetail(t);
}