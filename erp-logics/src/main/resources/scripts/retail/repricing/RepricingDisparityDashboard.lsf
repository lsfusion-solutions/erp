MODULE RepricingDisparityDashboard;

REQUIRE DisparityDashboard, RepricingDisparity;

NAMESPACE Disparity;

overCreateDisparityItemStock(item, stock, detail) += ACTION (item, stock, detail) {
    afterInputRetailPriceDisparityDetail(detail) <- disparityPriceItemStock(item, stock);
} 

