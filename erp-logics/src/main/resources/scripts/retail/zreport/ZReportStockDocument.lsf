MODULE ZReportStockDocument;

REQUIRE ZReport, StockDocumentSkuLedger;

// ----------------------------------- Проведение по товарному отчету -------------------------- //

@implementStockDocumentLedgerOut(ZReport, departmentStore);
sumOutStockDocumentLedger (ledger) += sumSkuReceiptDetailZReport(ledger);
sumItemOutStockDocumentLedger (ledger) += sumSkuReceiptDetailZReport(ledger);
sumContainerOutStockDocumentLedger (ledger) += 0.0 IF ledger IS ZReport;
 
