MODULE POSMachinery;

REQUIRE POS, MachineryPriceTransaction;

NAMESPACE POS;

overPriceSkuReceiptDateTime(sku, receipt, dateTime) += transactionPriceBarcodeGroupMachinery(barcodeSku(sku), groupCashRegisterReceipt(receipt)) IF dateTime IS DATETIME;
