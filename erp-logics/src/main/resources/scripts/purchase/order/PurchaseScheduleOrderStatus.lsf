MODULE PurchaseScheduleOrderStatus;

REQUIRE PurchaseScheduleOrder,
        PurchaseOrderStatus;

NAMESPACE Purchase;

EXTEND FORM scheduleOrderDetails
    PROPERTIES (o) READONLY  BEFORE isPosted(o) selected EDITABLE, isSent, isConfirmed
    PROPERTIES (o) READONLY  AFTER isPosted(o) status  BACKGROUND backgroundStatus(o)
    PROPERTIES(o) emailCustomer  
;