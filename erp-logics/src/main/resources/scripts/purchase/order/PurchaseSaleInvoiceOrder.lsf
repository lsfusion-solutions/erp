MODULE PurchaseSaleInvoiceOrder;

REQUIRE PurchaseShipment, SaleOrder;

NAMESPACE Purchase;

overBatch(InvoiceDetail detail)= OVERRIDE batch(detail), ledgerBatch(invoiceShipmentDetail(detail));  

@defineInvoiceOrder(' (продажа)', Sale, overBatch, customer, customerStock);