MODULE PurchaseDemandSeparation;

REQUIRE PurchaseDemand, PurchaseOrderSeparation;

NAMESPACE Purchase;

limitRecommendedQuantity (Sku s, UserOrder o) += separation(o) == separation(supplier(o), s) OR 
                                                 (NOT separation(o) AND NOT nullSeparation(supplier(o), s)); 
