MODULE PurchaseShipmentTax;

REQUIRE PurchaseShipment, SkuLedgerTax;

NAMESPACE Purchase;

VAT (ShipmentBatch b) += VAT(invoiceDetail(shipmentDetail(b)));
valueVAT (ShipmentBatch b) += valueVAT(invoiceDetail(shipmentDetail(b)));