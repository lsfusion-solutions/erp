MODULE PurchaseShipmentTax;

REQUIRE PurchaseShipment, SkuLedgerTax;

NAMESPACE Purchase;

VATBatch (batch) += VATInvoiceDetail(invoiceDetailBatch(batch));
valueVATBatch (batch) += valueVATInvoiceDetail(invoiceDetailBatch(batch));