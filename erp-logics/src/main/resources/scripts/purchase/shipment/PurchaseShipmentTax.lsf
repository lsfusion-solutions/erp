MODULE PurchaseShipmentTax;

REQUIRE PurchaseShipment, SkuLedgerTax;

NAMESPACE Purchase;

VAT (Batch batch) += VAT(invoiceDetail(batch));
valueVAT (Batch batch) += valueVAT(invoiceDetail(batch));