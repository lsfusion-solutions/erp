MODULE PurchaseShipmentPriceListStock;

REQUIRE PurchaseShipment, PurchaseInvoicePriceListStock;

NAMESPACE Purchase;

inPriceListLedgerBatch (ledger, batch) += TRUE IF inInvoiceDetailBatch(invoiceDetailInvoicePriceListLedger(ledger), batch); 
