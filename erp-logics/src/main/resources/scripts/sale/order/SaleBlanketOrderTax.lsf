MODULE SaleBlanketOrderTax;

REQUIRE SaleBlanketOrder, SkuLedgerTax;

NAMESPACE Sale;

overVATBlanketOrderDetail(detail) += VATBatch(batchBlanketOrderDetail(detail));
overSetValueVATBlanketOrderDetail(detail) += valueVATBatch(batchBlanketOrderDetail(detail));
