MODULE SkuLedgerTax;

REQUIRE SkuLedger, TaxItem;

NAMESPACE Stock;

valueVATBatchDefaultCountryCurrentDate 'НДС, %' (batch) = valueVATItemDefaultCountryCurrentDate(skuBatch(batch));

EXTEND FORM batches 
    PROPERTIES (bt) READONLY valueVATBatchDefaultCountryCurrentDate AFTER nameStockBatch(bt)
;    