MODULE NewLandBoard;

REQUIRE System, PriceChecker;

portNewLandBoard 'Порт NewLand' () = DATA INTEGER ();        
        
EXTEND FORM options PROPERTIES() portNewLandBoard;

DESIGN options {
    machinery {
        MOVE PROPERTY (portNewLandBoard());
    }
}