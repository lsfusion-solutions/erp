MODULE PurchaseSaleCertificate;

REQUIRE SaleCertificate, PurchaseCertificate, PurchaseSaleInvoice;

NAMESPACE Purchase;

overFillInvoice (Sale.UserInvoiceDetail sd, InvoiceDetail id) += { certificateText (sd) <- certificateText(id); }