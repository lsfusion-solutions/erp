MODULE Set10;

REQUIRE Integration, Item;


urlServerSet10 'Адрес:порт сервера SET10' = DATA STRING ();

EXTEND FORM integrationData
    PROPERTIES urlServerSet10();

DESIGN integrationData{
    pane {
        NEW set10 {
            caption = 'SET10';
            MOVE PROPERTY (urlServerSet10());
        }
    }
}

extraSet10id = ABSTRACT STRING[100](Item);
overSet10Id(Item i) = OVERRIDE extraSet10id(i), overId(i);