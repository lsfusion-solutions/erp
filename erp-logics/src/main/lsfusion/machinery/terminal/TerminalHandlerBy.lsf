MODULE TerminalHandlerBy;

REQUIRE TerminalHandler, ItemTracking;

NAMESPACE TerminalHandler;

in(Sku s, Stock st, TerminalBarcodeFlags f) += WHEN st IS Stock AND f == TerminalBarcodeFlags.isTracking
    AND isTrackingOver(s) AND currentDate() >= dateStartSPT(s) THEN TRUE;
