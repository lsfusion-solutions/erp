MODULE TerminalTeamWork;

REQUIRE Terminal;

NAMESPACE Terminal;

process(INTEGER command, FILE file) {

    fileToString(file);
    logToFile('terminal', 'file: ' + resultString());
    
}

