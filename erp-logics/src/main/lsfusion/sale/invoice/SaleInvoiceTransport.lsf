MODULE SaleInvoiceTransport;

REQUIRE SaleInvoice, Transport;

NAMESPACE Sale;

@defineDocumentTransport(userInvoice, i, headerTabbedPane);

truck = ABSTRACT VALUE Truck (Invoice);
truck (UserInvoice i) += truck(i);