MODULE PurchaseOrderLegalEntityManager;

REQUIRE PurchaseOrder, LegalEntityManager;

NAMESPACE Purchase;

filterManager (Order o) = filterManager(supplier(o));

EXTEND FORM orders
    FILTERS filterManager(o)
;