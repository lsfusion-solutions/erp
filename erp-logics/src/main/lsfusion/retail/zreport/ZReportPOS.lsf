MODULE ZReportPOS;

REQUIRE ZReport;

NAMESPACE ZReport;

shiftStart 'Время открытия' = DATA DATETIME (ZReport);
shiftEnd 'Время закрытия' = DATA DATETIME (ZReport);

EXTEND FORM zReports
    PROPERTIES READONLY shiftStart(z) AFTER time(z), shiftEnd(z) BEFORE nameDepartmentStore(z)
;
