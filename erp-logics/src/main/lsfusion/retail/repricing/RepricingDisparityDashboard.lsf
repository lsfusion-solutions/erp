MODULE RepricingDisparityDashboard;

REQUIRE DisparityDashboard, RepricingDisparity;

NAMESPACE Disparity;

overCreateItemStock(Item item, Stock stock, DisparityDetail detail) + { 
    afterInputRetailPrice(detail) <- disparityPrice(item, stock);
} 

