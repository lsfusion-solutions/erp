MODULE RangeItemManager;

REQUIRE Range, ItemManager;

NAMESPACE Range;

EXTEND FORM ranges 
    FILTERS countAccessItem(currentUser(), sg) OR NOT sg IS SkuGroup
;

EXTEND FORM range 
    FILTERS countAccessItem(currentUser(), sg) OR NOT sg IS SkuGroup
;