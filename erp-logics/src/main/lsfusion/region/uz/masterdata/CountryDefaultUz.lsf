MODULE CountryDefaultUz;

REQUIRE CountryDefault;

NAMESPACE Country;

loadDefaultData() + { 
    defaultCountry() <- country('860');
    currency(defaultCountry()) <- currencyShortName('UZS');
}
