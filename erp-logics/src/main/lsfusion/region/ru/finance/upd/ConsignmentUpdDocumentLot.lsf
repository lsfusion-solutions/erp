MODULE ConsignmentUpdDocumentLot;

REQUIRE UpdDocumentLot, ConsignmentUpdDocument, SaleShipmentLot;

NAMESPACE Upd;

overFill(UpdDocumentDetail d, Sale.InvoiceDetail id) + {
    FOR quantity(id, Lot lot) NEW ld = UpdLotDetail DO {
        updDocumentDetail(ld) <- d;
        id(ld) <- id(lot);
    }   
}


