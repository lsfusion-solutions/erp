MODULE EGAISProductType;

REQUIRE EGAIS;

NAMESPACE EGAIS;

CLASS ProductType 'Вид продукции ЕГАИС';
TABLE productType (ProductType);

id 'Код' = DATA STRING[5] (ProductType);
productType = GROUP AGGR ProductType t BY id(t);

name 'Наименование' = DATA ISTRING (ProductType) CHARWIDTH 25;

FORM productType 'Вид продукции ЕГАИС'
    OBJECTS o = ProductType PANEL
    PROPERTIES(o) name, id
    
    EDIT ProductType OBJECT o
;

FORM productTypes 'Вид продукции ЕГАИС'
    OBJECTS o = ProductType
    PROPERTIES(o) READONLY name, id
    PROPERTIES(o) NEWSESSION NEW, EDIT, DELETE
;

FORM dialogProductTypes 'Вид продукции ЕГАИС'
    OBJECTS o = ProductType
    PROPERTIES(o) READONLY name, id
    
    LIST ProductType OBJECT o
;

NAVIGATOR {
    EGAISMasterData {
        NEW productTypes;
    }
}

onStarted() + {
    IF NOT GROUP SUM 1 IF t IS ProductType THEN {
        NEWSESSION {
            NEW t = ProductType { id(t) <- '010'; name(t) <- 'Этиловый спирт-сырец из пищевого сырья '; };
            NEW t = ProductType { id(t) <- '020'; name(t) <- 'Этиловый спирт ректификованный из пищевого сырья'; };
            NEW t = ProductType { id(t) <- '0201'; name(t) <- 'Другие этиловые спирты из пищевого сырья'; };
            NEW t = ProductType { id(t) <- '025'; name(t) <- 'Этиловый спирт ректификованный из непищевого растительного сырья'; };
            NEW t = ProductType { id(t) <- '030'; name(t) <- 'Этиловый спирт из ЭАФ'; };
            NEW t = ProductType { id(t) <- '031'; name(t) <- 'Этиловый спирт из головной фракции этилового спирта'; };
            NEW t = ProductType { id(t) <- '040'; name(t) <- 'Синтетический этиловый спирт'; };
            NEW t = ProductType { id(t) <- '0401'; name(t) <- 'Технический синтетический этиловый спирт'; };
            NEW t = ProductType { id(t) <- '050'; name(t) <- 'Абсолютированный этиловый спирт'; };
            NEW t = ProductType { id(t) <- '0501'; name(t) <- 'Абсолютированный этиловый спирт из пищевого сырья'; };
            NEW t = ProductType { id(t) <- '0502'; name(t) <- 'Абсолютированный технический этиловый спирт'; };
            NEW t = ProductType { id(t) <- '0503'; name(t) <- 'Технический гидролизный этиловый спирт'; };
            NEW t = ProductType { id(t) <- '060'; name(t) <- 'Иные спирты кроме денатурированных (гидролизный, технический и др.)'; };
            NEW t = ProductType { id(t) <- '070'; name(t) <- 'Этиловый спирт денатурированный из пищевого сырья'; };
            NEW t = ProductType { id(t) <- '071'; name(t) <- 'Биоэтанол из пищевого сырья'; };
            NEW t = ProductType { id(t) <- '080'; name(t) <- 'Этиловый спирт денатурированный из непищевого сырья'; };
            NEW t = ProductType { id(t) <- '081'; name(t) <- 'Биоэтанол из непищевого сырья'; };
            NEW t = ProductType { id(t) <- '090'; name(t) <- 'Фракция головная этилового спирта'; };
            NEW t = ProductType { id(t) <- '0901'; name(t) <- 'Головная фракция этилового спирта'; };
            NEW t = ProductType { id(t) <- '091'; name(t) <- 'Фракция головных и промежуточных примесей этилового спирта'; };
            NEW t = ProductType { id(t) <- '092'; name(t) <- 'Промежуточная фракция этилового спирта из пищевого сырья'; };
            NEW t = ProductType { id(t) <- '100'; name(t) <- 'Спирт этиловый по фармакопейным статьям'; };
            NEW t = ProductType { id(t) <- '101'; name(t) <- 'Фармацевтическая субстанция спирта этилового (этанол)'; };
            NEW t = ProductType { id(t) <- '102'; name(t) <- 'Спиртосодержащие лекарства для использования в медицине'; };
            NEW t = ProductType { id(t) <- '103'; name(t) <- 'Спиртосодержащие лекарства для ветеринарии'; };
            NEW t = ProductType { id(t) <- '104'; name(t) <- 'Спиртосодержащие медицинские изделия'; };
            NEW t = ProductType { id(t) <- '110'; name(t) <- 'Коньячный дистиллят'; };
            NEW t = ProductType { id(t) <- '1101'; name(t) <- 'Дистиллят винный для бренди (дистиллят коньячный)'; };
            NEW t = ProductType { id(t) <- '1102'; name(t) <- 'Дистиллят винный для бренди выдержанный'; };
            NEW t = ProductType { id(t) <- '1103'; name(t) <- 'Дистиллят винный для бренди молодой'; };
            NEW t = ProductType { id(t) <- '1104'; name(t) <- 'Дистилляты винные для бренди прочие'; };
            NEW t = ProductType { id(t) <- '1105'; name(t) <- 'Дистиллят коньячный полного цикла производства'; };
            NEW t = ProductType { id(t) <- '120'; name(t) <- 'Кальвадосный дистиллят'; };
            NEW t = ProductType { id(t) <- '1201'; name(t) <- 'Дистилляты яблочные'; };
            NEW t = ProductType { id(t) <- '1202'; name(t) <- 'Дистиллят яблочный выдержанный (кальвадосный)'; };
            NEW t = ProductType { id(t) <- '1203'; name(t) <- 'Дистиллят яблочный молодой для яблочного бренди'; };
            NEW t = ProductType { id(t) <- '1204'; name(t) <- 'Другие дистилляты кальвадосные'; };
            NEW t = ProductType { id(t) <- '130'; name(t) <- 'Винный дистиллят'; };
            NEW t = ProductType { id(t) <- '1301'; name(t) <- 'Дистилляты винные'; };
            NEW t = ProductType { id(t) <- '1302'; name(t) <- 'Дистиллят винный ректификованный'; };
            NEW t = ProductType { id(t) <- '1303'; name(t) <- 'Другие винные дистилляты'; };
            NEW t = ProductType { id(t) <- '1304'; name(t) <- 'Дистилляты винные полного цикла производства'; };
            NEW t = ProductType { id(t) <- '1305'; name(t) <- 'Дистиллят винный полного цикла производства ректификованный с объемной долей спирта более 86%'; };
            NEW t = ProductType { id(t) <- '1306'; name(t) <- 'Дистилляты винные полного цикла производства прочие'; };
            NEW t = ProductType { id(t) <- '140'; name(t) <- 'Спирт этиловый питьевой'; };
            NEW t = ProductType { id(t) <- '150'; name(t) <- 'Виноградный дистиллят'; };
            NEW t = ProductType { id(t) <- '1501'; name(t) <- 'Дистиллят виноградного происхождения'; };
            NEW t = ProductType { id(t) <- '1502'; name(t) <- 'Дистиллят виноградного происхождения ректификованный'; };
            NEW t = ProductType { id(t) <- '1503'; name(t) <- 'Дистиллят виноградного происхождения прочие'; };
            NEW t = ProductType { id(t) <- '1504'; name(t) <- 'Дистилляты виноградные полного цикла производства'; };
            NEW t = ProductType { id(t) <- '1505'; name(t) <- 'Дистиллят виноградный полного цикла производства ректификованный с объемной долей спирта более 86%'; };
            NEW t = ProductType { id(t) <- '1506'; name(t) <- 'Дистилляты виноградные полного цикла производства прочие'; };
            NEW t = ProductType { id(t) <- '160'; name(t) <- 'Плодовый дистиллят'; };
            NEW t = ProductType { id(t) <- '161'; name(t) <- 'Фруктовый плодовый дистиллят'; };
            NEW t = ProductType { id(t) <- '1611'; name(t) <- 'Дистилляты фруктовые'; };
            NEW t = ProductType { id(t) <- '1612'; name(t) <- 'Дистиллят фруктовый ректификованный'; };
            NEW t = ProductType { id(t) <- '1613'; name(t) <- 'Дистилляты фруктовые прочие'; };
            NEW t = ProductType { id(t) <- '170'; name(t) <- 'Висковый дистиллят'; };
            NEW t = ProductType { id(t) <- '1701'; name(t) <- 'Дистиллят виски'; };
            NEW t = ProductType { id(t) <- '180'; name(t) <- 'Зерновой дистиллят'; };
            NEW t = ProductType { id(t) <- '1801'; name(t) <- 'Дистиллят зерновой'; };
            NEW t = ProductType { id(t) <- '190'; name(t) <- 'Дистиллят ромовый'; };
            NEW t = ProductType { id(t) <- '1901'; name(t) <- 'Дистилляты прочие полного цикла производства'; };
            NEW t = ProductType { id(t) <- '1902'; name(t) <- 'Дистилляты прочие'; };
            NEW t = ProductType { id(t) <- '1903'; name(t) <- 'Дистиллят медовый'; };
            NEW t = ProductType { id(t) <- '1904'; name(t) <- 'Дистилляты прочие'; };
            NEW t = ProductType { id(t) <- '200'; name(t) <- 'Водка'; };
            NEW t = ProductType { id(t) <- '2001'; name(t) <- 'Водка с защищенным наименованием места происхождения товара'; };
            NEW t = ProductType { id(t) <- '2002'; name(t) <- 'Водка особая'; };
            NEW t = ProductType { id(t) <- '2003'; name(t) <- 'Водка особая с защищенным наименованием места происхождения товара'; };
            NEW t = ProductType { id(t) <- '2004'; name(t) <- 'Водки прочие'; };
            NEW t = ProductType { id(t) <- '211'; name(t) <- 'Ликероводочные изделия с содержанием этилового спирта до 25% включительно'; };
            NEW t = ProductType { id(t) <- '2111'; name(t) <- 'Ликеро-водочные изделия с содержанием этилового спирта до 25% включительно (до 01.01.2013)'; };
            NEW t = ProductType { id(t) <- '2112'; name(t) <- 'Пунши'; };
            NEW t = ProductType { id(t) <- '2113'; name(t) <- 'Пунши прочие'; };
            NEW t = ProductType { id(t) <- '2114'; name(t) <- 'Наливки'; };
            NEW t = ProductType { id(t) <- '2115'; name(t) <- 'Напитки десертные'; };
            NEW t = ProductType { id(t) <- '212'; name(t) <- 'Ликероводочные изделия свыше 25% об.'; };
            NEW t = ProductType { id(t) <- '2121'; name(t) <- 'Крем'; };
            NEW t = ProductType { id(t) <- '2122'; name(t) <- 'Пунш с ромом'; };
            NEW t = ProductType { id(t) <- '2123'; name(t) <- 'Аперитивы'; };
            NEW t = ProductType { id(t) <- '2124'; name(t) <- 'Коктейли'; };
            NEW t = ProductType { id(t) <- '2125'; name(t) <- 'Бальзамы'; };
            NEW t = ProductType { id(t) <- '2126'; name(t) <- 'Ликероводочные изделия прочие'; };
            NEW t = ProductType { id(t) <- '2127'; name(t) <- 'Настойки'; };
            NEW t = ProductType { id(t) <- '2128'; name(t) <- 'Настойка горькая'; };
            NEW t = ProductType { id(t) <- '2129'; name(t) <- 'Настойка полусладкая'; };
            NEW t = ProductType { id(t) <- '21210'; name(t) <- 'Настойка сладкая'; };
            NEW t = ProductType { id(t) <- '21211'; name(t) <- 'Настойки прочие'; };
            NEW t = ProductType { id(t) <- '21212'; name(t) <- 'Ликеры'; };
            NEW t = ProductType { id(t) <- '21213'; name(t) <- 'Ликер крепкий'; };
            NEW t = ProductType { id(t) <- '21214'; name(t) <- 'Ликер десертный'; };
            NEW t = ProductType { id(t) <- '21215'; name(t) <- 'Ликер эмульсионный'; };
            NEW t = ProductType { id(t) <- '21216'; name(t) <- 'Ликер яичный'; };
            NEW t = ProductType { id(t) <- '21217'; name(t) <- 'Ликеры прочие'; };
            NEW t = ProductType { id(t) <- '229'; name(t) <- 'Коньяк и арманьяк в бутылках'; };
            NEW t = ProductType { id(t) <- '2291'; name(t) <- 'Коньяк'; };
            NEW t = ProductType { id(t) <- '2292'; name(t) <- 'Напиток крепкий из дистиллята винного для бренди (дистиллята коньячного) (коньяк)'; };
            NEW t = ProductType { id(t) <- '230'; name(t) <- 'Коньяки в бутылках'; };
            NEW t = ProductType { id(t) <- '231'; name(t) <- 'Коньяки обработанные для розлива в других организациях или промпереработки'; };
            NEW t = ProductType { id(t) <- '232'; name(t) <- 'Бренди'; };
            NEW t = ProductType { id(t) <- '2321'; name(t) <- 'Бренди высокого качества'; };
            NEW t = ProductType { id(t) <- '2322'; name(t) <- 'Бренди высокого качества выдержанный'; };
            NEW t = ProductType { id(t) <- '2323'; name(t) <- 'Бренди высокого качества старый'; };
            NEW t = ProductType { id(t) <- '2324'; name(t) <- 'Бренди высокого качества очень старый'; };
            NEW t = ProductType { id(t) <- '2325'; name(t) <- 'Бренди высокого качества коллекционный'; };
            NEW t = ProductType { id(t) <- '2326'; name(t) <- 'Бренди высокого качества с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '2327'; name(t) <- 'Бренди иные'; };
            NEW t = ProductType { id(t) <- '233'; name(t) <- 'Коньяк с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '2331'; name(t) <- 'Напиток крепкий из дистиллята винного для бренди (дистиллята коньячного) с защищенным географическим указанием (национальный коньяк)'; };
            NEW t = ProductType { id(t) <- '234'; name(t) <- 'Коньяк с защищенным наименованием места происхождения'; };
            NEW t = ProductType { id(t) <- '2341'; name(t) <- 'Коньяк с защищенным наименованием места происхождения товара'; };
            NEW t = ProductType { id(t) <- '235'; name(t) <- 'Коньяк коллекционный'; };
            NEW t = ProductType { id(t) <- '2351'; name(t) <- 'Коньяк особый'; };
            NEW t = ProductType { id(t) <- '236'; name(t) <- 'Ромы'; };
            NEW t = ProductType { id(t) <- '237'; name(t) <- 'Виски'; };
            NEW t = ProductType { id(t) <- '238'; name(t) <- 'Напитки коньячные до 25% об. включительно'; };
            NEW t = ProductType { id(t) <- '239'; name(t) <- 'Напитки коньячные свыше 25% об.'; };
            NEW t = ProductType { id(t) <- '241'; name(t) <- 'Напитки коньячные, бренди до 25% об. включительно'; };
            NEW t = ProductType { id(t) <- '242'; name(t) <- 'Напитки коньячные, бренди свыше 25% об.'; };
            NEW t = ProductType { id(t) <- '250'; name(t) <- 'Напитки винные до 25% об. включительно'; };
            NEW t = ProductType { id(t) <- '251'; name(t) <- 'Напитки винные свыше 25% об.'; };
            NEW t = ProductType { id(t) <- '252'; name(t) <- 'Кальвадосы'; };
            NEW t = ProductType { id(t) <- '2521'; name(t) <- 'Кальвадосы Российские'; };
            NEW t = ProductType { id(t) <- '2522'; name(t) <- 'Напиток крепкий из дистиллята яблочного выдержанного (кальвадосного) (кальвадос)'; };
            NEW t = ProductType { id(t) <- '2523'; name(t) <- 'Кальвадосы прочие'; };
            NEW t = ProductType { id(t) <- '260'; name(t) <- 'Слабоалкогольная продукция'; };
            NEW t = ProductType { id(t) <- '2601'; name(t) <- 'Напитки слабоалкогольные'; };
            NEW t = ProductType { id(t) <- '2602'; name(t) <- 'Напитки слабоалкогольные прочие'; };
            NEW t = ProductType { id(t) <- '2603'; name(t) <- 'Напитки алкогольные медовые за исключением медовухи'; };
            NEW t = ProductType { id(t) <- '2604'; name(t) <- 'Напиток слабоалкогольный спиртованный'; };
            NEW t = ProductType { id(t) <- '2605'; name(t) <- 'Другие слабоалкогольные напитки'; };
            NEW t = ProductType { id(t) <- '2606'; name(t) <- 'Сидр фруктовый'; };
            NEW t = ProductType { id(t) <- '2607'; name(t) <- 'Сидр фруктовый ароматизированный'; };
            NEW t = ProductType { id(t) <- '2608'; name(t) <- 'Напитки слабоалкогольные брожения прочие'; };
            NEW t = ProductType { id(t) <- '261'; name(t) <- 'Сидр'; };
            NEW t = ProductType { id(t) <- '2611'; name(t) <- 'Сидр ароматизированный'; };
            NEW t = ProductType { id(t) <- '262'; name(t) <- 'Пуаре'; };
            NEW t = ProductType { id(t) <- '263'; name(t) <- 'Медовуха'; };
            NEW t = ProductType { id(t) <- '270'; name(t) <- 'Другие спиртные напитки до 25% об. включительно'; };
            NEW t = ProductType { id(t) <- '280'; name(t) <- 'Другие спиртные напитки свыше 25% об.'; };
            NEW t = ProductType { id(t) <- '2801'; name(t) <- 'Водка виноградная'; };
            NEW t = ProductType { id(t) <- '2802'; name(t) <- 'Водка виноградная с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '2803'; name(t) <- 'Водка виноградная с защищенным наименованием места происхождения товара'; };
            NEW t = ProductType { id(t) <- '2804'; name(t) <- 'Водки фруктовые (плодовые)'; };
            NEW t = ProductType { id(t) <- '2805'; name(t) <- 'Водки виноградные прочие'; };
            NEW t = ProductType { id(t) <- '2806'; name(t) <- 'Водка медовая'; };
            NEW t = ProductType { id(t) <- '2807'; name(t) <- 'Джины'; };
            NEW t = ProductType { id(t) <- '2808'; name(t) <- 'Джин дистиллированный'; };
            NEW t = ProductType { id(t) <- '2809'; name(t) <- 'Джин сухой'; };
            NEW t = ProductType { id(t) <- '28010'; name(t) <- 'Джины прочие'; };
            NEW t = ProductType { id(t) <- '28011'; name(t) <- 'Бренди фруктовый'; };
            NEW t = ProductType { id(t) <- '28012'; name(t) <- 'Бренди фруктовый коллекционный'; };
            NEW t = ProductType { id(t) <- '28013'; name(t) <- 'Напиток спиртной из зернового сырья'; };
            NEW t = ProductType { id(t) <- '28014'; name(t) <- 'Прочие напитки спиртные'; };
            NEW t = ProductType { id(t) <- '28015'; name(t) <- 'Спиртные напитки прочие'; };
            NEW t = ProductType { id(t) <- '300'; name(t) <- 'Коньячные спирты'; };
            NEW t = ProductType { id(t) <- '310'; name(t) <- 'Кальвадосные спирты'; };
            NEW t = ProductType { id(t) <- '320'; name(t) <- 'Виноматериалы'; };
            NEW t = ProductType { id(t) <- '321'; name(t) <- 'Виноматериалы виноградные'; };
            NEW t = ProductType { id(t) <- '3211'; name(t) <- 'Виноматериалы виноградные столовые'; };
            NEW t = ProductType { id(t) <- '3212'; name(t) <- 'Виноматериалы виноградные ликерные'; };
            NEW t = ProductType { id(t) <- '322'; name(t) <- 'Виноматериалы фруктовые'; };
            NEW t = ProductType { id(t) <- '3221'; name(t) <- 'Виноматериалы фруктовые столовые'; };
            NEW t = ProductType { id(t) <- '3222'; name(t) <- 'Виноматериалы фруктовые сброженные'; };
            NEW t = ProductType { id(t) <- '323'; name(t) <- 'Специальные виноматериалы виноградные'; };
            NEW t = ProductType { id(t) <- '3231'; name(t) <- 'Виноматериалы виноградные специальные'; };
            NEW t = ProductType { id(t) <- '3232'; name(t) <- 'Виноматериалы виноградные прочие'; };
            NEW t = ProductType { id(t) <- '324'; name(t) <- 'Специальные виноматериалы фруктовые'; };
            NEW t = ProductType { id(t) <- '3241'; name(t) <- 'Виноматериалы фруктовые специальные'; };
            NEW t = ProductType { id(t) <- '3242'; name(t) <- 'Виноматериалы фруктовые прочие'; };
            NEW t = ProductType { id(t) <- '330'; name(t) <- 'Соки спиртованные'; };
            NEW t = ProductType { id(t) <- '3301'; name(t) <- 'Морсы спиртованные'; };
            NEW t = ProductType { id(t) <- '331'; name(t) <- 'Соки сброженно-спиртованные'; };
            NEW t = ProductType { id(t) <- '340'; name(t) <- 'Дистилляты'; };
            NEW t = ProductType { id(t) <- '341'; name(t) <- 'Фруктовое сусло'; };
            NEW t = ProductType { id(t) <- '3411'; name(t) <- 'Сусла фруктовые'; };
            NEW t = ProductType { id(t) <- '342'; name(t) <- 'Медовое сусло'; };
            NEW t = ProductType { id(t) <- '3421'; name(t) <- 'Сусло медовое'; };
            NEW t = ProductType { id(t) <- '343'; name(t) <- 'Пивное сусло'; };
            NEW t = ProductType { id(t) <- '3431'; name(t) <- 'Сусло пивное'; };
            NEW t = ProductType { id(t) <- '344'; name(t) <- 'Виноградное сусло'; };
            NEW t = ProductType { id(t) <- '3441'; name(t) <- 'Сусло виноградное'; };
            NEW t = ProductType { id(t) <- '345'; name(t) <- 'Спиртованное виноградное сусло (мистель)'; };
            NEW t = ProductType { id(t) <- '3451'; name(t) <- 'Сусло виноградное спиртованное (мистель)'; };
            NEW t = ProductType { id(t) <- '346'; name(t) <- 'Сусло фруктовое спиртованное'; };
            NEW t = ProductType { id(t) <- '3461'; name(t) <- 'Сусло фруктовое сброженное'; };
            NEW t = ProductType { id(t) <- '3462'; name(t) <- 'Сусло фруктовое сброженно-спиртованное'; };
            NEW t = ProductType { id(t) <- '3463'; name(t) <- 'Сусла фруктовые прочие'; };
            NEW t = ProductType { id(t) <- '350'; name(t) <- 'Другая спиртосодержащая пищевая продукция'; };
            NEW t = ProductType { id(t) <- '3501'; name(t) <- 'Настои спиртованные'; };
            NEW t = ProductType { id(t) <- '3502'; name(t) <- 'Спирт ароматный'; };
            NEW t = ProductType { id(t) <- '3503'; name(t) <- 'Продукция спиртосодержащая пищевая прочая'; };
            NEW t = ProductType { id(t) <- '400'; name(t) <- 'Вина натуральные'; };
            NEW t = ProductType { id(t) <- '401'; name(t) <- 'Вино (виноградное)'; };
            NEW t = ProductType { id(t) <- '4011'; name(t) <- 'Вино'; };
            NEW t = ProductType { id(t) <- '402'; name(t) <- 'Вино с защищенным географическим указанием или с защищенным наименованием места происхождения'; };
            NEW t = ProductType { id(t) <- '403'; name(t) <- 'Вино виноградное столовое'; };
            NEW t = ProductType { id(t) <- '4031'; name(t) <- 'Вино столовое'; };
            NEW t = ProductType { id(t) <- '4032'; name(t) <- 'Вино молодое'; };
            NEW t = ProductType { id(t) <- '4033'; name(t) <- 'Вино под пленкой'; };
            NEW t = ProductType { id(t) <- '4034'; name(t) <- 'Вино сортовое'; };
            NEW t = ProductType { id(t) <- '4035'; name(t) <- 'Вино тихое'; };
            NEW t = ProductType { id(t) <- '4036'; name(t) <- 'Вина столовые прочие'; };
            NEW t = ProductType { id(t) <- '4037'; name(t) <- 'Херес'; };
            NEW t = ProductType { id(t) <- '404'; name(t) <- 'Вино с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '406'; name(t) <- 'Вино коллекционное виноградное'; };
            NEW t = ProductType { id(t) <- '4061'; name(t) <- 'Вино выдержанное'; };
            NEW t = ProductType { id(t) <- '4062'; name(t) <- 'Вино коллекционное'; };
            NEW t = ProductType { id(t) <- '410'; name(t) <- 'Вина (за исключением натуральных, игристых и шампанских) <*>'; };
            NEW t = ProductType { id(t) <- '411'; name(t) <- 'Ликерное вино'; };
            NEW t = ProductType { id(t) <- '4111'; name(t) <- 'Кагор'; };
            NEW t = ProductType { id(t) <- '4112'; name(t) <- 'Мадера'; };
            NEW t = ProductType { id(t) <- '4113'; name(t) <- 'Портвейн'; };
            NEW t = ProductType { id(t) <- '4114'; name(t) <- 'Вина крепленые прочие'; };
            NEW t = ProductType { id(t) <- '412'; name(t) <- 'Ликерное вино с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '4121'; name(t) <- 'Вино ликерное с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '413'; name(t) <- 'Ликерное вино с защищенным названием места происхождения'; };
            NEW t = ProductType { id(t) <- '4131'; name(t) <- 'Вино ликерное с защищенным названием места происхождения'; };
            NEW t = ProductType { id(t) <- '414'; name(t) <- 'Вино столовое с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '415'; name(t) <- 'Вино столовое с защищенным названием места происхождения'; };
            NEW t = ProductType { id(t) <- '4151'; name(t) <- 'Вино столовое с защищенным названием места происхождения'; };
            NEW t = ProductType { id(t) <- '416'; name(t) <- 'Вино столовое коллекционное'; };
            NEW t = ProductType { id(t) <- '420'; name(t) <- 'Вина плодовые'; };
            NEW t = ProductType { id(t) <- '421'; name(t) <- 'Фруктовое плодовое вино'; };
            NEW t = ProductType { id(t) <- '4211'; name(t) <- 'Вино фруктовое ликерное'; };
            NEW t = ProductType { id(t) <- '4212'; name(t) <- 'Фрукты в фруктовом вине'; };
            NEW t = ProductType { id(t) <- '4213'; name(t) <- 'Вино фруктовое столовое'; };
            NEW t = ProductType { id(t) <- '4214'; name(t) <- 'Вино фруктовое газированное'; };
            NEW t = ProductType { id(t) <- '4215'; name(t) <- 'Вино фруктовое газированное жемчужное'; };
            NEW t = ProductType { id(t) <- '4216'; name(t) <- 'Вино фруктовое крепленое'; };
            NEW t = ProductType { id(t) <- '4217'; name(t) <- 'Вино фруктовое крепленое ароматизированное'; };
            NEW t = ProductType { id(t) <- '4218'; name(t) <- 'Вино фруктовое крепленое по специальной технологии'; };
            NEW t = ProductType { id(t) <- '4219'; name(t) <- 'Вино фруктовое игристое'; };
            NEW t = ProductType { id(t) <- '42110'; name(t) <- 'Вино фруктовое игристое жемчужное'; };
            NEW t = ProductType { id(t) <- '42111'; name(t) <- 'Вина фруктовые игристые прочие'; };
            NEW t = ProductType { id(t) <- '422'; name(t) <- 'Вино коллекционное фруктовое'; };
            NEW t = ProductType { id(t) <- '4221'; name(t) <- 'Вино фруктовое коллекционное'; };
            NEW t = ProductType { id(t) <- '430'; name(t) <- 'Вино специальное с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '4301'; name(t) <- 'Вино специальное с защищенным географическим указанием (для производителей республики Крым и города Севастополь)'; };
            NEW t = ProductType { id(t) <- '4302'; name(t) <- 'Вино специальное с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '431'; name(t) <- 'Вино специальное с защищенным наименованием места происхождения'; };
            NEW t = ProductType { id(t) <- '4311'; name(t) <- 'Вино специальное с защищенным наименованием места происхождения товара. (для производителей республики Крым и города Севастополь)'; };
            NEW t = ProductType { id(t) <- '4312'; name(t) <- 'Вино специальное с защищенным наименованием места происхождения'; };
            NEW t = ProductType { id(t) <- '4313'; name(t) <- 'Вина ликерные прочие'; };
            NEW t = ProductType { id(t) <- '432'; name(t) <- 'Вино ароматизированное виноградное'; };
            NEW t = ProductType { id(t) <- '4321'; name(t) <- 'Вермут'; };
            NEW t = ProductType { id(t) <- '4322'; name(t) <- 'Вина прочие ароматизированные включая из свежего винограда'; };
            NEW t = ProductType { id(t) <- '440'; name(t) <- 'Вино игристое'; };
            NEW t = ProductType { id(t) <- '4401'; name(t) <- 'Вино игристое высокого качества'; };
            NEW t = ProductType { id(t) <- '4402'; name(t) <- 'Вина игристые жемчужные'; };
            NEW t = ProductType { id(t) <- '441'; name(t) <- 'Вино игристое с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '4411'; name(t) <- 'Вино игристое виноградное шампанское с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '4422'; name(t) <- 'Вино игристое виноградное шампанское с защищенным наименованием места происхождения товара'; };
            NEW t = ProductType { id(t) <- '442'; name(t) <- 'Вина игристые с защищенным наименованием места происхождения'; };
            NEW t = ProductType { id(t) <- '4421'; name(t) <- 'Вино игристое с защищенным наименованием места происхождения товара'; };
            NEW t = ProductType { id(t) <- '443'; name(t) <- 'Вино игристое коллекционное'; };
            NEW t = ProductType { id(t) <- '450'; name(t) <- 'Вина шампанские'; };
            NEW t = ProductType { id(t) <- '4501'; name(t) <- 'Вино игристое виноградное шампанское'; };
            NEW t = ProductType { id(t) <- '451'; name(t) <- 'Вина шампанские с защищенным географическим указанием'; };
            NEW t = ProductType { id(t) <- '452'; name(t) <- 'Вина шампанские с защищенным наименованием места происхождения'; };
            NEW t = ProductType { id(t) <- '453'; name(t) <- 'Вина шампанские коллекционные'; };
            NEW t = ProductType { id(t) <- '4531'; name(t) <- 'Вино шампанское коллекционное'; };
            NEW t = ProductType { id(t) <- '460'; name(t) <- 'Другие вина'; };
            NEW t = ProductType { id(t) <- '461'; name(t) <- 'Винный напиток, произведенный без добавления этилового спирта'; };
            NEW t = ProductType { id(t) <- '4611'; name(t) <- 'Винные напитки виноградные, произведенные без добавления этилового спирта без насыщения двуокисью углерода'; };
            NEW t = ProductType { id(t) <- '4612'; name(t) <- 'Вино газированное'; };
            NEW t = ProductType { id(t) <- '4613'; name(t) <- 'Вина газированные, в том числе из свежего винограда'; };
            NEW t = ProductType { id(t) <- '4614'; name(t) <- 'Вино газированное жемчужное'; };
            NEW t = ProductType { id(t) <- '4615'; name(t) <- 'Вина газированные прочие'; };
            NEW t = ProductType { id(t) <- '4616'; name(t) <- 'Винные напитки виноградные, произведенные без добавления этилового спирта с насыщением двуокисью углерода'; };
            NEW t = ProductType { id(t) <- '4617'; name(t) <- 'Напитки винные фруктовые, произведенные без добавления этилового спирта'; };
            NEW t = ProductType { id(t) <- '4618'; name(t) <- 'Напиток винный газированный'; };
            NEW t = ProductType { id(t) <- '4619'; name(t) <- 'Напиток винный газированный жемчужный'; };
            NEW t = ProductType { id(t) <- '46110'; name(t) <- 'Напитки винные фруктовые ароматизированные'; };
            NEW t = ProductType { id(t) <- '46111'; name(t) <- 'Напитки винные фруктовые газированные'; };
            NEW t = ProductType { id(t) <- '462'; name(t) <- 'Винный напиток, произведенный с добавлением этилового спирта'; };
            NEW t = ProductType { id(t) <- '4621'; name(t) <- 'Винные напитки виноградные, произведенные с добавлением этилового спирта без насыщения двуокисью углерода'; };
            NEW t = ProductType { id(t) <- '4622'; name(t) <- 'Винные напитки виноградные, произведенные с добавлением этилового спирта с насыщением двуокисью углерода'; };
            NEW t = ProductType { id(t) <- '4623'; name(t) <- 'Напитки винные фруктовые, произведенные с добавлением этилового спирта'; };
            NEW t = ProductType { id(t) <- '4624'; name(t) <- 'Напиток винный крепкий (из дистиллята коньячного)'; };
            NEW t = ProductType { id(t) <- '4625'; name(t) <- 'Напиток винный ароматизированный'; };
            NEW t = ProductType { id(t) <- '4626'; name(t) <- 'Напиток виноградный крепкий'; };
            NEW t = ProductType { id(t) <- '4627'; name(t) <- 'Напитки виноградосодержащие из виноградного сырья ароматизированные'; };
            NEW t = ProductType { id(t) <- '4628'; name(t) <- 'Напиток фруктовый крепкий'; };
            NEW t = ProductType { id(t) <- '4629'; name(t) <- 'Напитки винные фруктовые прочие'; };
            NEW t = ProductType { id(t) <- '46210'; name(t) <- 'Коктейли винные'; };
            NEW t = ProductType { id(t) <- '481'; name(t) <- 'Парфюмерно-косметическая продукция'; };
            NEW t = ProductType { id(t) <- '482'; name(t) <- 'Товары бытовой химии'; };
            NEW t = ProductType { id(t) <- '483'; name(t) <- 'Спиртосодержащие растворы'; };
            NEW t = ProductType { id(t) <- '484'; name(t) <- 'Другая спиртосодержащая непищевая продукция'; };
            NEW t = ProductType { id(t) <- '4841'; name(t) <- 'Продукция спиртосодержащая непищевая прочая'; };
            NEW t = ProductType { id(t) <- '485'; name(t) <- 'Денатурированная спиртосодержащая непищевая продукция'; };
            NEW t = ProductType { id(t) <- '4851'; name(t) <- 'Продукция денатурированная спиртосодержащая непищевая'; };
            NEW t = ProductType { id(t) <- '500'; name(t) <- 'Пиво с содержанием объемной доли этилового спирта свыше 0,5% и до 8,6% включительно'; };
            NEW t = ProductType { id(t) <- '510'; name(t) <- 'Пиво с содержанием объемной доли этилового спирта свыше 8,6%'; };
            NEW t = ProductType { id(t) <- '520'; name(t) <- 'Напитки, изготавливаемые на основе пива (напитки пивные)'; };
            NEW t = ProductType { id(t) <- '530'; name(t) <- 'Напиток алкогольный медовый'; };
            NEW t = ProductType { id(t) <- '531'; name(t) <- 'Напиток алкогольный медовый газированный'; };
            NEW t = ProductType { id(t) <- '532'; name(t) <- 'Напиток алкогольный медовый крепленый'; };
            NEW t = ProductType { id(t) <- '533'; name(t) <- 'Напиток алкогольный медовый десертный'; };
            NEW t = ProductType { id(t) <- '534'; name(t) <- 'Напиток алкогольный медовый нектар'; };
            NEW t = ProductType { id(t) <- '535'; name(t) <- 'Напиток алкогольный медовый столовый'; };
            NEW t = ProductType { id(t) <- '536'; name(t) <- 'Напитки алкогольные медовые за исключением медовухи прочие'; };
            NEW t = ProductType { id(t) <- '537'; name(t) <- 'Напиток медовый крепкий'; };
            NEW t = ProductType { id(t) <- '538'; name(t) <- 'Винодельческая продукция прочая'; };
            APPLY;
        }
    }
}
