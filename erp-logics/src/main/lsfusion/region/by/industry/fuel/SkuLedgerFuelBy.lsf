MODULE SkuLedgerFuelBy;

REQUIRE SkuLedgerFuel, ItemFuelBy, Store;

NAMESPACE Stock;

notShowFuelVolume(Stock st) += st IS DepartmentStore;

DESIGN skuLedger {
    PROPERTY (signedVolume(s)) { hide = TRUE; }
    PROPERTY (density(s)) { hide = TRUE; }
}