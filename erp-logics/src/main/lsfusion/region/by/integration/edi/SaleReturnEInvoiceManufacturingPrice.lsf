MODULE SaleReturnEInvoiceManufacturingPrice;

REQUIRE SaleReturnEInvoice, SaleReturnManufacturingPrice;

NAMESPACE SaleReturn;

overCopy(EInvoiceDetail ei, UserInvoiceDetail i) + {
    manufacturingPrice(i) <- NUMERIC[16,4](replace(lineItemExtraField(ei, 'Отпускная цена'),',','.'));
    //manufacturingMarkup(i) <- NUMERIC[8,3](replace(lineItemExtraField(ei, 'Оптовая надбавка'),',','.'));
    //OVERRIDE lineItemExtraField(ei, 'Пояснения к ценообразованию'), additionalInformation(ei);
}