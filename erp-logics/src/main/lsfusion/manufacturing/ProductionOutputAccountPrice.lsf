MODULE ProductionOutputAccountPrice;

REQUIRE ProductionOutput, SkuLedgerCost, ProductionOutputAccountLedger;

NAMESPACE Production;

accountPrice 'Учетная цена (после)' = DATA NUMERIC[16,4] (OutputDetail);
accountLedgerPrice (OrderOutputDetail d) += accountPrice(d);

@defineCalcAveragePriceBalance(OutputDetail, stock, 'производстве');

@defineTabAccountPrice(manufacturing, od, OutputDetail, 'Производство');