MODULE PurchaseShipmentCorrectionGestori;

REQUIRE PurchaseShipmentCorrection, LegalEntityRu;

NAMESPACE Purchase;

calcShipmentPriceA (InvoiceCorrectionDetail detail) += WHEN accountInvoicePrice(legalEntity(customerStock(detail))) THEN invoicePriceA(detail);
